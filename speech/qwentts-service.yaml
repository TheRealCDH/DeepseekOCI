apiVersion: v1
kind: Service
metadata:
  name: qwentts
  labels:
    app.kubernetes.io/name: qwentts
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: qwentts
  ports:
    - name: http
      port: 8080
      targetPort: 8080
