apiVersion: v1
kind: Service
metadata:
  name: speaches
  labels:
    app.kubernetes.io/name: speaches
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: speaches
  ports:
    - name: http
      port: 8000
      targetPort: 8000
